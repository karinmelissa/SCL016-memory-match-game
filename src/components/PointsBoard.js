const Board = () =>{
    const puntos = document.createElement('p');
    //const node = document.createTextNode("Cantidad de puntos");
    //puntos.appendChild(node);

    return puntos;
}
export default Board;